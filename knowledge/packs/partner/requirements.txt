# Partner.skill — Python 依赖
# 核心功能仅需 Python 3 标准库，以下为可选增强依赖

# 中文姓名转 slug（可选，缺失时自动 fallback 到简单处理）
pypinyin>=0.50.0

# 数据分析与可视化（可选，用于生成 RQI 雷达图）
matplotlib>=3.7.0
numpy>=1.24.0

# PDF 生成（可选，用于导出关系报告）
reportlab>=4.0.0

# 自然语言处理（可选，用于聊天记录情感分析增强）
# jieba>=0.42.1

# 测试
pytest>=7.0.0
